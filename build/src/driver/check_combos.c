/* THIS FILE IS AUTOMATICALLY GENERATED BY table */

#include <stddef.h>
#include "options.h"
#include "option_seen.h"
#include "option_names.h"
#include "opt_actions.h"
#include "errors.h"

/* replace individual options with combo */
static void
replace_with_combo (int combo_index)
{
	int flag;
	int count = 1;
	FOREACH_OPTION_IN_COMBO(flag,combo_index) {
		if (count == 1) {
			replace_option_seen(flag, combo_index);
		} else {
			set_option_unseen(flag);
		}
		count++;
	}
}

static void
report_combo_errors (void)
{
}

extern boolean
is_replacement_combo (int combo_index)
{
	switch (combo_index) {
	default:
		return FALSE;
	}
}

extern void
check_for_combos (void)
{
	report_combo_errors();
}

